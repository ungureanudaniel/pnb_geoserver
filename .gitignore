# Environment secrets — never commit these
.env

# GeoServer data directory — large files, managed on server
geoserver/data/

# SSL certificates — private key must never be committed
nginx/ssl/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# development yml
docker-compose.dev.yml

# development nginx
nginx/nginx.dev.conf
